(ns streamcraft.migration-datomic.api-test
  (:require [clojure.test :as test :refer :all]
            [streamcraft.migration-datomic.api :as migration-datomic]))

(deftest dummy-test
  (is (= 1 1)))
