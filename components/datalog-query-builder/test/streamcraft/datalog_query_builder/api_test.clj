(ns streamcraft.datalog-query-builder.api-test
  (:require [clojure.test :as test :refer :all]
            [streamcraft.datalog-query-builder.api :as datalog-query-builder]))

(deftest dummy-test
  (is (= 1 1)))
