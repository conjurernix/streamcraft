(ns streamcraft.redis-store-carmine.api-test
  (:require [clojure.test :as test :refer :all]
            [streamcraft.redis-store-carmine.api :as redis-store-carmine]))

(deftest dummy-test
  (is (= 1 1)))
