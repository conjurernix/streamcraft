(ns streamcraft.email-client-mailgun.api-test
  (:require [clojure.test :as test :refer :all]
            [streamcraft.email-client-mailgun.api :as email-client-mailgun]))

(deftest dummy-test
  (is (= 1 1)))
